{
  "name": "vidyut",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:relay": "bun build ./src/relay/cli.ts --compile --outfile dist/vidyut-relay",
    "install:relay": "bash scripts/install-relay.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "multicast-dns": "^7.2.5",
    "qrcode-terminal": "^0.12.0"
  }
}
