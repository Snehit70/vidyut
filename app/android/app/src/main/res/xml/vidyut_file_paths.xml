<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches the directory ReceivedImageRepository writes under the app
         support (files) dir; only this subtree is exposed via content URIs. -->
    <files-path name="vidyut_received" path="vidyut_received/" />
    <cache-path name="vidyut_cache" path="." />
    <external-files-path name="vidyut_external" path="." />
</paths>
